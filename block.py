from pico2d import *


class Block():
    def __init__(self):
        #블록들에 필요한 변수를 선언한다.
    def draw(self):
        #블록들의 총 개수만큼 출력해준다.
    def set(self):
        #블록들에게 좌표를 지정해준다.
    def block_return(self):
        #메인 파일에게 블록들의 좌표를 넘겨준다.(충돌체크를 위함)
        

